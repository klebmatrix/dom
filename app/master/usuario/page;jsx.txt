'use client'

import Link from 'next/link'
import { useEffect } from 'react'

export default function MasterPage() {
  useEffect(() => {
    console.log('MasterPage loaded')
  }, [])

  const cards = [
    { nome: 'Gerenciar Usuários', href: '/master/usuarios' },
    { nome: 'Ferramentas de Rede', href: '/master/rede' },
    { nome: 'Ferramentas de Sistema', href: '/master/sistema' },
    { nome: 'Gráficos e Métricas', href: '/master/graficos' },
  ]

  return (
    <div className="min-h-screen bg-gray-100 flex flex-col items-center justify-center px-4">
      <h1 className="text-3xl font-bold mb-8">Painel Master</h1>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6 w-full max-w-3xl">
        {cards.map((card, idx) => (
          <Link key={idx} href={card.href}>
            <div className="bg-white shadow-md rounded-xl p-6 hover:bg-blue-100 cursor-pointer text-center transition-all">
              <h2 className="text-xl font-semibold">{card.nome}</h2>
            </div>
          </Link>
        ))}
      </div>
    </div>
  )
}
